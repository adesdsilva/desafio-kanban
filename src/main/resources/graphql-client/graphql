type Project {
  id: ID!
  name: String!
  status: String
  plannedStart: String
  plannedEnd: String
  actualStart: String
  actualEnd: String
}

type Query {
  projects: [Project!]!
  project(id: ID!): Project
}

type Mutation {
  createProject(name: String!): Project
}
